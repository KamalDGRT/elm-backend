### Why add schema using pydantic models

- We use pydantic models as a part of data validation.
- It is because of this we can ensure that whatever data is sent by the frontend is in compliance with the backend.
